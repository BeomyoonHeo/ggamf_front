import 'package:flutter/material.dart';

class JoinPartyList extends StatelessWidget {
  const JoinPartyList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
