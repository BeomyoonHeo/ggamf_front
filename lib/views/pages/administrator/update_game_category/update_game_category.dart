import 'package:flutter/material.dart';

class UpdateGameCategory extends StatelessWidget {
  const UpdateGameCategory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
